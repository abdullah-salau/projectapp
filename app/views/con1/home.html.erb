<br>
<br>
<h1 class="text-center home-heading">Welcome to the DePauw</h1>
<h1 class="text-center home-heading">CompSci Club Website!</h1>
<br>
<br>
<%= image_tag "laptop1.jpg", alt: "some txt", height: "20%", width: "90%", :style => "margin: 0% 5% 0% 5%" %>


<br>
<br>
<h1 class="text-center">Upcoming Events</h1>
<hr style="width: 60%">
  <br>
    <% if @events %>
      <% @events.each do |e|%>
          <div class="col-md-6" style="padding-left: 5%">
            <div class="event-item">
              <ul class="list-group event-date">
                <li class="list-group-item list-group-item-success"><%=e.month%></li>
                <li class="list-group-item day"><h2><b><%=e.day%></b></h2></li>
              </ul>
              <h3><%=e.title%></h3>
              </div>
          </div>
            
     <% end %>
    <% end %>




<h1 class="text-center" style="clear:both">Latest Articles</h1>
<hr style="width: 60%">
  <br>
<!--We include here articles from the blog using for loop to display 2 or 3 articles; we also use if statement incase
there are no articles, whereby we print No artilces to show. We need to create article model and connect it to user model
so that 1 user can have multiple articles. we can use the dcc logo for each list item, and style the title,
name, and article content accordingly,
we might need to change the latest articles from div to <a> -->

<% if @user1s %>
  <% @user1s.each do |u|%>
      <% if u.articles %>
        <% u.articles.each do |a| %>
          <% if @count<2 %>
              <div class="row row-element">
                <%= image_tag "profilePic.png", alt: "some txt", height: "7.5%", width: "11.25%", :style => "float: left; margin-right: 3%" %>
                <h2><%= a.title %></h2><h3>by <%= u.name %></h3><p><%= a.content %></p>

                </div>
                <br>
                <% @count = @count + 1 %>
          <% else %>
          <% end %>
       <% end %>
      <% end %>
 <% end %>
<% end %>
<br>
